# iLogic-Inventor — Альбом каменных изделий

## Что это

iLogic правило для Autodesk Inventor, которое автоматически собирает IDW-альбом из 3D-моделей каменных изделий (`.ipt`).

- Читает Excel-файл со списком моделей (лист `ALBUM`, через COM automation `Excel.Application`, поэтому на машине должен быть установлен Microsoft Excel)
- Для каждой модели создаёт лист A3 альбомный в IDW
- Рисует рамку СПДС (Форма 3) через `AddBorder`
- Применяет штамп (TitleBlock) через `AddTitleBlock` с данными из Excel
- Размещает адаптивный набор видов по шаблону: минимум 2 ортогональных вида и, если подходит шаблон, дополнительно изометрию и вспомогательный вид.

## Основной файл

`docs/iLogic/StoneAlbumRule.ilogic.vb` — **текущая рабочая версия**

Вставить в Inventor: Управление → iLogic → Правила → Создать → вставить код → запустить из открытого `.idw`.

## Дополнительные правила

- `docs/iLogic/DiagBorderTest.ilogic.vb` — диагностика наличия рамки и штампа в документе
- `docs/iLogic/IsoDimensionsFromIptRule.ilogic.vb` — отдельное правило для подтягивания габаритов из `.ipt/.iam` и добавления подписи рядом с изометрическими видами

## Константы в коде (критически важные)

```
BORDER_NAME = "RKM_SPDS_A3_BORDER_V12"   ← имя рамки в Образец.idw
TB_NAME     = "RKM_SPDS_A3_FORM3_V17"    ← имя штампа в Образец.idw
SHEET_PFX   = "ALB_"                     ← префикс создаваемых листов
```

Если в документе нет этих определений — правило создаёт их программно, не перезаписывая уже существующие ресурсы.

## Excel (ALBUM-RKM.xlsx)

Лист `ALBUM`. Колонки:
- `MODEL_PATH` — путь к `.ipt` (относительный от папки workspace или абсолютный)
- `CODE`, `PROJECT_NAME`, `DRAWING_NAME`, `ORG_NAME`, `STAGE` — поля штампа
- Номера листов `SHEET`/`SHEETS` заполняются автоматически

## Параметры модели в `.ipt` (для корректного авто-подхвата)

Чтобы правила стабильно подтягивали геометрию и подписи, в модели рекомендуется иметь параметры:

- `L` — длина детали (мм, чистовой размер)
- `W` — ширина детали (мм, чистовой размер)
- `H` — толщина/высота детали (мм, чистовой размер)

Если параметров нет, правило использует геометрию модели (`RangeBox`/вершины тела) и вычисляет:
- orientation-agnostic габариты в плане (minimum bounding rectangle),
- толщину по оси Z.

Дополнительно поддерживаются custom iProperties:
- `StoneDensityKgM3` (по умолчанию `2800`) — для оценки массы,
- `TechAllowancePerSideMm` (по умолчанию `5`) — технологический припуск на сторону для заготовки.

## Критические ограничения iLogic (НЕ НАРУШАТЬ)

1. **NO** inline `Try : код : Catch : End Try` на одной строке — каждый оператор на отдельной строке
2. `Alias` — зарезервировано VB.NET, не использовать как имя переменной
3. `shared` — зарезервировано, не использовать
4. **Нет** `Imports System.IO` — только полные имена: `System.IO.Path.`, `System.IO.File.`
5. **Нет** `Imports System.Text.RegularExpressions` — только `System.Text.RegularExpressions.Regex.`
6. `sheet.Border` и `sheet.TitleBlock` — **ReadOnly**, присваивать нельзя
7. `AddCustomBorder` **НЕ СУЩЕСТВУЕТ** в iLogic → правильный метод: **`AddBorder`**
8. `SilentOperation = True` нужен вокруг `AddBorder` и `AddTitleBlock`

## Архитектура кода

```
Main()
  └── AlbumBuilder.Build()
        ├── EnsureBorder(doc)      ← создаёт BorderDefinition при отсутствии, существующую не трогает
        ├── EnsureTitleBlock(doc)  ← создаёт TitleBlockDefinition при отсутствии, существующую не трогает
        ├── PurgeAlbumSheets()     ← удаляет старые ALB_* листы
        └── BuildOneSheet()        ← для каждой модели из Excel
              ├── doc.Sheets.Add() → лист ALB_NNN / ALB_NNN_2 / ... при коллизиях имён
              ├── sheet.AddBorder(borderDef)           [SilentOperation=True]
              ├── sheet.AddTitleBlock(tbDef, , ps)     [SilentOperation=True]
              ├── Documents.Open(modelPath)
              └── PlaceViewsSlotBased()
                    ├── MeasureView × 4 (probe-виды на масштабе 0.1)
                    ├── 6 перестановок → выбрать лучший матчинг вид→слот
                    ├── PlaceViewInSlot × 4
                    └── AddDimNotes × 4 (текстовые ноты с размерами в мм)
```

## Геометрия слотов (А3, абсолютные значения мм)

```
Рабочая зона по текущей реализации берётся внутри рамки: X=[20, 415], Y=[65, 292]  (над штампом 55мм). Правая граница не уменьшается на ширину штампа по всей высоте листа.

┌────────────────────────────────────────────────────┐ Y=292
│  SMALL (20..119, 229..292)  │  WIDE (124..415, 229..292)  │
├───────────────────────┬────────────────────────────┤ Y=229
│                       │                            │
│   LARGE               │    ISO                     │
│  (20..276, 65..224)   │  (281..415, 65..224)       │
│                       │                            │
└───────────────────────┴────────────────────────────┘ Y=65
X=20                  X=281                        X=415
```

## Статус (последнее обновление: март 2026)

| Что | Статус |
|-----|--------|
| Рамка AddBorder | ✅ работает |
| Штамп AddTitleBlock | ✅ работает (ps массив 7 элементов, 0-based) |
| Чтение Excel (`Excel.Application` COM automation) | ✅ работает, требуется установленный Microsoft Excel |
| Layout видов | ✅ rule подбирает шаблон и размещает минимум 2 ортогональных вида; обычно добавляет изометрию, но не гарантирует строго «4 вида всегда» для каждого случая |
| Размеры на видах | ✅ DrawingNotes с мм |
| Подтягивание габаритов из .ipt/.iam после расстановки изометрии | ✅ отдельное правило `IsoDimensionsFromIptRule` |

## VBA-источники (legacy, только для справки)

`legacy_vba/` содержит неполные исторические фрагменты и reference-excerpts для сверки логики, а не гарантированно импортируемый полный VBA-проект. Файлы сохранены как справочный материал, часть вспомогательных процедур в них опущена.
